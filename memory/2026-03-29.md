# Daily Memory - 2026-03-29 (凌晨补充)

## 极空间 NAS（ZSpace）连接配置

**IP**: 192.168.3.134
**SSH端口**: 10000（用户名 18664974664，密码 Wly19940728）
**WebDAV端口**: 5005（HTTP文件共享）
**SMB端口**: 445

**WebDAV读写测试成功**，但SSH免密登录还差一步：
- 公钥已写入 `/home/18664974664/.ssh/authorized_keys`
- 用户 home 目录配置为 `/home/`（不是 `/home/18664974664/`）
- SSH 在 `/home/.ssh/authorized_keys` 找 key，但该目录无写权限
- **✅ 已解决**：LeonWan 在 ZSpace 终端执行了 `ln -s /home/18664974664/.ssh /home/.ssh`，SSH 免密已通
- **SSH 固化配置**：`~/.ssh/config` 中添加了 `Host znas`，以后直接 `ssh znas` 免密登录

**WebDAV已可用**：
- `curl -X PROPFIND -u "18664974664:Wly19940728" "http://192.168.3.134:5005/"`
- 共享目录：sata1（SATA硬盘）、nvme11（NVMe SSD）、public、sharetome
- NAS总计1755个项目（1696文件+59目录）
- 主存储在 `/sata1-18664974664/`，有迅雷下载、iPhone备份等

**工具脚本**：`~/scripts/nas.sh`（list/get/put命令）

**ZSpace管理后台**：
- 通过SSH隧道 `ssh -L 8080:127.0.0.1:80 -p 10000 ...` 访问 `http://127.0.0.1:8080`
- Web管理后台需要JS渲染，API需要token认证
- 已知API路径：`/auth/login`、`/setting/sshd/status`、`/auth/tmptoken`
- 远程协助token（临时，过期）格式：`expire_at`, `nasid`, `ctime`, `pwd`

## 日报Cron配置修复

- **sessionTarget**: `isolated` → `main`（让cron在有飞书权限的主session运行）
- **Schedule**: `0 18 * * *` → `0 18 * * 1-5`（周一到周五，周末跳过）
- 下次触发：周一 2026-03-30 18:00

## Obsidian目录整理（2026-03-28完成）

`07-Clippings/` 下新建目录：
- 📅 日报/（daily-report系列）
- 📱 小红书/（XHS周报）
- 🎓 学习剪藏/（文章笔记）
- 💼 工作剪藏/（已有）
- 📋 剪藏/（模板，已有）

Git post-commit hook已设置（自动push到GitHub）

## 其他

- ZSpace NAS型号：Z4Pro-EQP7（Intel平台，ZOS系统）
- 存储：/dev/bcache0 (3.7T SATA) + /dev/bcache1 (3.7T SATA) + /dev/nvme0n1p4 (908G NVMe)
- `DISPLAY=:0 SSH_ASKPASS` trick 可绕过SSH密码认证（每次需重新认证）

## SSH 免密登录成功（2026-03-29 凌晨）

- LeonWan 在 ZSpace 终端执行 `ln -s /home/18664974664/.ssh /home/.ssh`，解决 SSH home 目录问题
- 助手公钥写入 `/home/18664974664/.ssh/authorized_keys`，SSH 免密登录成功
- `ssh znas` 直接免密连接（~/.ssh/config 已配置）
- **备选方式**：`ssh 18664974664@192.168.3.134 -p 10000`（密码方式，ZSpace后来放开了密码认证）
- Docker 在 NAS 上运行（dockerd），但用户不在 docker 组，无法直接 `docker ps/exec`
- Docker HTTP API 未对外开放，监听在 localhost:8080（被 openresty 占用）
- ZSpace 自有容器管理：`/zspace/applications/services/zdocker/zdocker`
- SSH 已添加到 TOOLS.md 和 MEMORY.md
